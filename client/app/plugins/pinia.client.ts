export default defineNuxtPlugin(() => {
  // This plugin ensures Pinia is properly initialized on the client side
  // The @pinia/nuxt module should handle most of this automatically
  // but this provides additional safety
}) 